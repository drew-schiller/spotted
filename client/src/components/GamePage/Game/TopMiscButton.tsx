import React from "react";
import styles from "./Game.module.sass";

type Props = {};

const TopMiscButton = (props: Props) => {
  return <div className={styles.topMiscButton}>?</div>;
};

export default TopMiscButton;
