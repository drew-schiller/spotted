import React from "react";
import styles from "./Game.module.sass";

type Props = {};

const PlaybackActionsButton = (props: Props) => {
  return <div className={styles.playbackActionsButton}></div>;
};

export default PlaybackActionsButton;
