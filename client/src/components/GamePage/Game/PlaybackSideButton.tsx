import React from "react";
import styles from "./Game.module.sass";

type Props = {};

const PlaybackSideButton = (props: Props) => {
  return <div className={styles.playbackSideButton}>btn</div>;
};

export default PlaybackSideButton;
